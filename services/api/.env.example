# Server
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Auth
JWT_SECRET=change-me-in-production-min-32-chars-long
JWT_EXPIRES_IN=7d

# Database — leave empty to run with in-memory repositories (zero infra)
# DATABASE_URL=postgres://trotxi:trotxi@localhost:5432/trotxi
DATABASE_URL=
