## What

<!-- One or two sentences: what does this PR change and why? -->

## How to verify

<!-- Commands or steps a reviewer can run. e.g. `make check`, `make e2e`, a screen to open. -->

## Checklist

- [ ] `make check` passes (typecheck + lint + unit tests)
- [ ] `make e2e` passes (Postgres up via `make up`)
- [ ] Flutter apps still analyze/test clean (if `apps/` changed)
- [ ] Docs updated if behaviour changed (README / CLAUDE.md)
